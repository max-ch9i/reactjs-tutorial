import React, { Component } from 'react';

class Title extends Component {
    render() {
        return(
            /*
             * Task 3. Display Spaceport as a title of the app
             *
             * Topics: JSX props. Help: https://facebook.github.io/react/docs/components-and-props.html#rendering-a-component
             *
             * TODO: Replace div with h1 and render prop text inside of it.
             */
            <div>Title</div>
        );
    }
}

export default Title;








// Hint to Task 3: props are stored in this.props object




















// Solution to Task 3: <h1>{this.props.text}</h1>
